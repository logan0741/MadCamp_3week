# Garment Template Meshes

Place template meshes here for size scaling:

- `top.obj`
- `pants.obj`
- `dress.obj`
- `skirt.obj`

If these files are missing, the pipeline falls back to SNUG mesh assets or a
simple primitive mesh.
